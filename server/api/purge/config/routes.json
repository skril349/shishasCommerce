{
  "routes": [
    {
      "method": "GET",
      "path": "/purges",
      "handler": "purge.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/purges/count",
      "handler": "purge.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/purges/:id",
      "handler": "purge.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/purges",
      "handler": "purge.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/purges/:id",
      "handler": "purge.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/purges/:id",
      "handler": "purge.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
